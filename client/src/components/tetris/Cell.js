import React from "react";
import "../styles/Cell.css";

const Cell = ({ cell }) => {
  return (
    <div className={`Cell ${cell.className}`}>
      {/*  */}
      {/*  */}
    </div>
  );
};

export default Cell;
