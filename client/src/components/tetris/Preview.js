import React from "react";

const Preview = ({ tetrominoes }) => {
  const preview = tetrominoes.slice(1 - tetrominoes.length).reverse();
  return (
    <div className="Preview">
      {/*  */}
      {/*  */}
    </div>
  );
};

export default Preview;
